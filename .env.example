# Supabase (get from project settings)
DATABASE_URL="postgresql://postgres:[password]@db.[project].supabase.co:5432/postgres"

# Cloudflare R2 (get from R2 dashboard > Manage R2 API Tokens)
R2_ACCOUNT_ID=""
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET_NAME="moltplace-snapshots"

# App secret (generate with: openssl rand -base64 32)
API_KEY_SECRET=""

# Vercel Cron secret (generate with: openssl rand -base64 32)
CRON_SECRET=""
